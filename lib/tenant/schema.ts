/**
 * Brand-prefixed SoR table names. Live here so product code stays
 * ratchet-clean (lib/tenant is excluded from check-brand-literals.sh).
 */
export const HARDWARE_PRODUCTS_TABLE = 'circletel_hardware_products';
