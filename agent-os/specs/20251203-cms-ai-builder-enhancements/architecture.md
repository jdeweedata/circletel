# CMS AI Builder Enhancements - Architecture

**Spec ID**: `20251203-cms-ai-builder-enhancements`

---

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        CMS AI BUILDER ARCHITECTURE                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐         │
│  │   Page Builder  │    │   AI Services   │    │    Supabase     │         │
│  │    (React)      │◄──►│    (Node.js)    │◄──►│   (Postgres)    │         │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘         │
│          │                      │                      │                    │
│          │                      │                      │                    │
│          ▼                      ▼                      ▼                    │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐         │
│  │  Zustand Store  │    │   Gemini 3 Pro  │    │   RLS Policies  │         │
│  │  (State Mgmt)   │    │   (AI Models)   │    │   (Security)    │         │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagrams

### Theme Generation Flow

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   User       │     │   Theme      │     │   Theme      │     │   Gemini     │
│   Action     │────►│   Controls   │────►│   API        │────►│   3 Pro      │
│              │     │   Component  │     │   Endpoint   │     │   Model      │
└──────────────┘     └──────────────┘     └──────────────┘     └──────────────┘
                            │                    │                    │
                            │                    │                    │
       1. Click "Generate"  │   2. POST /theme   │   3. AI Prompt     │
                            │                    │                    │
                            ▼                    ▼                    ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   Preview    │◄────│   Zustand    │◄────│   Theme      │◄────│   Color      │
│   Update     │     │   Store      │     │   Service    │     │   Palette    │
│              │     │              │     │              │     │   Response   │
└──────────────┘     └──────────────┘     └──────────────┘     └──────────────┘
       │
       ▼
┌──────────────┐
│   Apply or   │
│   Regenerate │
└──────────────┘
```

### SEO Scoring Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           SEO SCORING PIPELINE                              │
└─────────────────────────────────────────────────────────────────────────────┘

     Content Change
           │
           ▼
    ┌──────────────┐
    │   Debounce   │ (500ms)
    │   Trigger    │
    └──────────────┘
           │
           ▼
    ┌──────────────┐     ┌──────────────────────────────────────────────┐
    │   Collect    │     │                                              │
    │   Page Data  │────►│   Title, Meta, Blocks, Images, Keywords     │
    └──────────────┘     │                                              │
           │             └──────────────────────────────────────────────┘
           ▼
    ┌──────────────┐
    │   POST       │
    │   /seo-score │
    └──────────────┘
           │
           ▼
    ┌──────────────────────────────────────────────────────────────────┐
    │                      SEO CHECKS                                  │
    ├──────────────┬──────────────┬──────────────┬────────────────────┤
    │    Title     │     Meta     │   Headings   │   Images    │ Content│
    │  (30-60ch)   │  (120-160ch) │   (H1, H2s)  │  (alt text) │ (300w) │
    │   15 pts     │    15 pts    │    15 pts    │   15 pts    │ 15 pts │
    └──────────────┴──────────────┴──────────────┴─────────────┴────────┘
           │
           ▼
    ┌──────────────┐
    │   Calculate  │
    │   Weighted   │────► Score: 0-100
    │   Score      │────► Grade: A/B/C/D/F
    └──────────────┘────► Recommendations[]
           │
           ▼
    ┌──────────────┐     ┌──────────────┐
    │   Update     │     │   Save to    │
    │   UI Badge   │     │   History    │
    └──────────────┘     └──────────────┘
```

### AI Text Field Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        AI TEXT FIELD WORKFLOW                               │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌──────────────┐
    │   Text       │
    │   Field      │◄──── Current value
    └──────────────┘
           │
           │ Hover
           ▼
    ┌──────────────┐
    │   AI Button  │ ✨
    │   Appears    │
    └──────────────┘
           │
           │ Click
           ▼
    ┌──────────────────────────────────────────────────────────────────┐
    │                        AI MENU                                   │
    ├──────────────┬──────────────┬──────────────┬────────────────────┤
    │  Write New   │   Improve    │   Shorten    │      Expand        │
    │              │              │              │                    │
    │  "Generate   │  "Make this  │  "Make this  │  "Add more        │
    │   from       │   more       │   more       │   detail to       │
    │   scratch"   │   engaging"  │   concise"   │   this text"      │
    └──────────────┴──────────────┴──────────────┴────────────────────┘
           │
           │ Select Action
           ▼
    ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
    │   Build      │     │   POST       │     │   Gemini     │
    │   Context    │────►│   /generate  │────►│   3 Pro      │
    │   Prompt     │     │              │     │              │
    └──────────────┘     └──────────────┘     └──────────────┘
           │
           │ Context includes:
           │ - Field type (headline, description, cta)
           │ - Current value
           │ - Page context (industry, tone)
           │ - Site brief data
           │
           ▼
    ┌──────────────┐
    │   Streaming  │
    │   Response   │◄──── Text appears character by character
    └──────────────┘
           │
           ▼
    ┌──────────────────────────────────────────────────────────────────┐
    │                    PREVIEW MODE                                  │
    │   ┌────────────────────────────────────────────────────────┐    │
    │   │  Generated text shown with orange background           │    │
    │   └────────────────────────────────────────────────────────┘    │
    │                                                                  │
    │              [✓ Accept]              [✗ Reject]                 │
    └──────────────────────────────────────────────────────────────────┘
           │                    │
           │                    │
           ▼                    ▼
    ┌──────────────┐     ┌──────────────┐
    │   Apply to   │     │   Restore    │
    │   Field      │     │   Original   │
    └──────────────┘     └──────────────┘
```

---

## Component Architecture

### Builder Components

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           PAGE BUILDER LAYOUT                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                        BuilderHeader                                  │ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐        ┌───────┐ ┌────────────┐ │ │
│  │  │  Logo   │ │  Title  │ │  Status │        │  SEO  │ │   Actions  │ │ │
│  │  │         │ │  Input  │ │  Badge  │        │ Score │ │ Save|Publish│ │ │
│  │  └─────────┘ └─────────┘ └─────────┘        └───────┘ └────────────┘ │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌────────────┐ ┌────────────────────────────────────────┐ ┌─────────────┐ │
│  │            │ │                                        │ │             │ │
│  │   Block    │ │            BuilderCanvas               │ │ Properties  │ │
│  │   Palette  │ │                                        │ │   Panel     │ │
│  │            │ │  ┌──────────────────────────────────┐  │ │             │ │
│  │  ─────────  │ │  │  Block with SectionToggle       │  │ │  ─────────  │ │
│  │  Layout    │ │  │  [👁] Hero Section               │  │ │  Content   │ │
│  │  ─────────  │ │  └──────────────────────────────────┘  │ │  ─────────  │ │
│  │  Content   │ │                                        │ │             │ │
│  │  ─────────  │ │  ┌──────────────────────────────────┐  │ │ AITextField │ │
│  │  Media     │ │  │  [👁] Features Grid              │  │ │  ┌───────┐  │ │
│  │  ─────────  │ │  └──────────────────────────────────┘  │ │  │  ✨   │  │ │
│  │  Conversion│ │                                        │ │  └───────┘  │ │
│  │            │ │  ┌──────────────────────────────────┐  │ │             │ │
│  │            │ │  │  [👁] CTA Section                │  │ │  Settings  │ │
│  │            │ │  └──────────────────────────────────┘  │ │             │ │
│  │            │ │                                        │ │             │ │
│  └────────────┘ └────────────────────────────────────────┘ └─────────────┘ │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                         AI Tools Panel                                │ │
│  │  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐          │ │
│  │  │ ThemeControls   │ │ LayoutVariations│ │ SEOScorePanel   │          │ │
│  │  │                 │ │                 │ │                 │          │ │
│  │  │ [Shuffle]       │ │ [Variation 1]   │ │ Score: 85       │          │ │
│  │  │ [Generate]      │ │ [Variation 2]   │ │ Grade: A        │          │ │
│  │  │                 │ │ [Variation 3]   │ │ [View Details]  │          │ │
│  │  └─────────────────┘ └─────────────────┘ └─────────────────┘          │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Component Hierarchy

```
PageBuilder
├── BuilderHeader
│   ├── Logo
│   ├── TitleInput
│   ├── StatusBadge
│   ├── SEOScoreBadge ─────────────────┐
│   │   └── SEOScorePanel (expandable) │ NEW
│   └── ActionButtons                  │
│       └── SiteBriefButton ───────────┼─┐
│           └── SiteBriefEditor (modal)│ │ NEW
│                                      │ │
├── BlockPalette                       │ │
│   └── BlockCategories                │ │
│       └── BlockItem (draggable)      │ │
│                                      │ │
├── BuilderCanvas                      │ │
│   └── BlockList                      │ │
│       └── BlockWrapper               │ │
│           ├── SectionToggle ─────────┼─┘ NEW
│           ├── BlockContent           │
│           └── BlockActions           │
│                                      │
├── PropertiesPanel                    │
│   ├── ContentEditor                  │
│   │   └── AITextField ───────────────┘ NEW
│   └── SettingsEditor
│
└── AIToolsPanel
    ├── GenerateSection
    ├── EnhanceSection
    ├── ThemeControls ─────────────────── NEW
    ├── LayoutVariations ──────────────── NEW
    └── SEOSection
```

---

## Database Schema Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          DATABASE SCHEMA                                    │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────┐         ┌──────────────────────┐
│   cms_pages          │         │   cms_theme_presets  │
├──────────────────────┤         ├──────────────────────┤
│ id (PK)              │◄────────│ id (PK)              │
│ slug                 │         │ name                 │
│ title                │         │ slug                 │
│ content_type         │    ┌───►│ colors (JSONB)       │
│ status               │    │    │ fonts (JSONB)        │
│ content (JSONB)      │    │    │ industry             │
│ seo_metadata (JSONB) │    │    │ mood                 │
│ theme                │    │    │ is_system            │
│ seo_score ───────────┼────┼────│ created_by           │
│ theme_preset_id ─────┼────┘    │ created_at           │
│ site_brief_id ───────┼────┐    └──────────────────────┘
│ author_id            │    │
│ version              │    │
│ created_at           │    │
│ updated_at           │    │
└──────────────────────┘    │
         │                  │
         │                  ▼
         │    ┌──────────────────────┐
         │    │   cms_site_briefs    │
         │    ├──────────────────────┤
         │    │ id (PK)              │
         │    │ page_id (FK) ────────┼────┐
         │    │ business_name        │    │
         │    │ industry             │    │
         │    │ target_audience      │    │
         │    │ value_proposition    │    │
         │    │ key_messages[]       │    │
         │    │ tone                 │    │
         │    │ theme_preferences    │    │
         │    │ layout_preferences   │    │
         │    │ section_visibility   │    │
         │    │ created_at           │    │
         │    │ updated_at           │    │
         │    └──────────────────────┘    │
         │                                │
         ▼                                │
┌──────────────────────┐                  │
│   cms_seo_scores     │                  │
├──────────────────────┤                  │
│ id (PK)              │                  │
│ page_id (FK) ────────┼──────────────────┘
│ score                │
│ checks (JSONB)       │
│ recommendations[]    │
│ calculated_at        │
└──────────────────────┘

┌──────────────────────┐
│ cms_layout_variations│
├──────────────────────┤
│ id (PK)              │
│ name                 │
│ description          │
│ block_types[]        │
│ structure (JSONB)    │
│ thumbnail_url        │
│ is_system            │
│ created_at           │
└──────────────────────┘
```

---

## API Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           API ENDPOINTS                                     │
└─────────────────────────────────────────────────────────────────────────────┘

/api/admin/cms/
├── theme/
│   └── route.ts
│       ├── POST ──► Generate theme (AI)
│       │           Shuffle theme
│       │           Save preset
│       └── GET  ──► List presets
│
├── layout/
│   └── route.ts
│       ├── POST ──► Generate variations (AI)
│       └── GET  ──► List preset layouts
│
├── seo-score/
│   └── route.ts
│       ├── POST ──► Calculate score
│       └── GET  ──► Get history
│
├── brief/
│   └── [pageId]/
│       └── route.ts
│           ├── GET  ──► Get brief
│           ├── PUT  ──► Update brief
│           └── DELETE ► Remove brief
│
└── generate/
    └── route.ts (existing)
        └── POST ──► Enhanced with theme/layout types


Request Flow:
┌────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│ Client │────►│   API    │────►│ Service  │────►│ Database │
│        │     │  Route   │     │  Layer   │     │  or AI   │
└────────┘     └──────────┘     └──────────┘     └──────────┘
     │              │                │                │
     │              │                │                │
     │    Auth      │    Validate    │    Business    │
     │    Check     │    Input       │    Logic       │
     │              │                │                │
     ▼              ▼                ▼                ▼
┌────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│Response│◄────│  Format  │◄────│  Result  │◄────│   Data   │
│        │     │  Output  │     │          │     │          │
└────────┘     └──────────┘     └──────────┘     └──────────┘
```

---

## State Management

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        ZUSTAND STORE STRUCTURE                              │
└─────────────────────────────────────────────────────────────────────────────┘

usePageBuilderStore
│
├── State
│   ├── currentPage
│   │   ├── id, slug, title, content_type, status
│   │   ├── content: { blocks: Block[] }
│   │   ├── seo_metadata
│   │   ├── theme_settings ◄──── NEW
│   │   │   ├── primaryColor
│   │   │   ├── secondaryColor
│   │   │   ├── accentColor
│   │   │   ├── backgroundColor
│   │   │   └── textColor
│   │   ├── seo_score ◄───────── NEW
│   │   └── site_brief_id ◄───── NEW
│   │
│   ├── blocks[]
│   │   └── Block
│   │       ├── id, type, content, settings
│   │       └── isVisible ◄───── NEW (for section toggle)
│   │
│   ├── siteBrief ◄───────────── NEW
│   │   ├── businessName
│   │   ├── industry
│   │   ├── targetAudience
│   │   ├── valueProposition
│   │   ├── keyMessages[]
│   │   ├── tone
│   │   ├── themePreferences
│   │   ├── layoutPreferences
│   │   └── sectionVisibility
│   │
│   ├── seoScore ◄────────────── NEW
│   │   ├── score
│   │   ├── grade
│   │   ├── checks{}
│   │   └── recommendations[]
│   │
│   └── UI state...
│
└── Actions
    ├── Page actions (existing)
    ├── Block actions (existing)
    │
    ├── Theme actions ◄───────── NEW
    │   ├── setTheme(theme)
    │   ├── shuffleTheme()
    │   └── applyThemePreset(presetId)
    │
    ├── Visibility actions ◄──── NEW
    │   ├── toggleBlockVisibility(blockId)
    │   └── setAllBlocksVisibility(visible)
    │
    ├── Site brief actions ◄──── NEW
    │   ├── setSiteBrief(brief)
    │   └── updateSiteBrief(updates)
    │
    └── SEO actions ◄─────────── NEW
        ├── setSEOScore(score)
        └── refreshSEOScore()
```

---

## Integration Points

### Existing System Integration

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      INTEGRATION WITH EXISTING CMS                          │
└─────────────────────────────────────────────────────────────────────────────┘

Existing                              New
────────                              ───

lib/cms/ai-service.ts    ◄───────────► Enhanced with theme/layout prompts
                         │
lib/cms/store.ts         ◄───────────► New theme, visibility, SEO actions
                         │
lib/cms/types.ts         ◄───────────► New type definitions
                         │
components/admin/cms/    ◄───────────► New components + integrations
PropertiesPanel.tsx                    Uses AITextField
BuilderCanvas.tsx                      Uses SectionToggle
BuilderHeader.tsx                      Uses SEOScoreBadge
AIGeneratorPanel.tsx                   Uses ThemeControls, LayoutVariations


API Pattern:
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   Existing   │     │   Shared     │     │     New      │
│   Endpoints  │◄───►│   Patterns   │◄───►│   Endpoints  │
│              │     │              │     │              │
│ /generate    │     │ Auth check   │     │ /theme       │
│ /pages       │     │ Rate limit   │     │ /layout      │
│ /templates   │     │ Error handle │     │ /seo-score   │
│ /media       │     │ Validation   │     │ /brief       │
└──────────────┘     └──────────────┘     └──────────────┘
```

---

## Security Model

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          SECURITY ARCHITECTURE                              │
└─────────────────────────────────────────────────────────────────────────────┘

Authentication Flow:
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Client  │────►│  Supabase│────►│   RLS    │────►│   Data   │
│  Request │     │   Auth   │     │  Policy  │     │  Access  │
└──────────┘     └──────────┘     └──────────┘     └──────────┘

RLS Policy Matrix:
┌─────────────────────┬─────────┬─────────┬─────────┬─────────┐
│ Table               │ SELECT  │ INSERT  │ UPDATE  │ DELETE  │
├─────────────────────┼─────────┼─────────┼─────────┼─────────┤
│ cms_theme_presets   │ Own+Sys │ Admin   │ Admin   │ Admin   │
│ cms_site_briefs     │ Own     │ Own     │ Own     │ Own     │
│ cms_seo_scores      │ Own     │ Service │ Service │ Admin   │
│ cms_layout_variations│ System │ Admin   │ Admin   │ Admin   │
└─────────────────────┴─────────┴─────────┴─────────┴─────────┘

Rate Limiting:
┌────────────────────────────────────────────────────────────┐
│ Endpoint          │ Limit        │ Window    │ By         │
├───────────────────┼──────────────┼───────────┼────────────┤
│ POST /theme       │ 10 requests  │ 1 hour    │ User ID    │
│ POST /layout      │ 10 requests  │ 1 hour    │ User ID    │
│ POST /seo-score   │ 100 requests │ 1 hour    │ User ID    │
│ POST /generate    │ 50 requests  │ 1 hour    │ User ID    │
└───────────────────┴──────────────┴───────────┴────────────┘
```

---

## Performance Considerations

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       PERFORMANCE OPTIMIZATIONS                             │
└─────────────────────────────────────────────────────────────────────────────┘

1. Caching Strategy:
   ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
   │   Request    │────►│    Cache     │────►│   Database   │
   │              │     │   (5 min)    │     │   (if miss)  │
   └──────────────┘     └──────────────┘     └──────────────┘

   - Theme presets: Cache for 1 hour
   - SEO scores: Cache for 5 minutes (per page version)
   - Layout variations: Cache for 30 minutes

2. Debouncing:
   - SEO score calculation: 500ms debounce
   - AI text generation: 300ms debounce
   - Theme preview: Immediate (no AI call)

3. Lazy Loading:
   - Site brief editor: Load on demand
   - SEO detail panel: Load on expand
   - Layout variations: Generate on request

4. Optimistic Updates:
   - Theme shuffle: Immediate UI update
   - Block visibility: Immediate toggle
   - Accept AI text: Immediate apply

Performance Targets:
┌──────────────────────────────────────────────────────────────┐
│ Operation              │ Target    │ Max       │ Notes      │
├────────────────────────┼───────────┼───────────┼────────────┤
│ Theme shuffle          │ < 50ms    │ 100ms     │ Client-side│
│ Theme generation       │ < 2s      │ 5s        │ AI call    │
│ Layout generation      │ < 3s      │ 8s        │ AI call    │
│ SEO score calculation  │ < 200ms   │ 500ms     │ Algorithm  │
│ AI text generation     │ < 2s      │ 5s        │ Streaming  │
│ Site brief save        │ < 300ms   │ 1s        │ Database   │
└──────────────────────────────────────────────────────────────┘
```
