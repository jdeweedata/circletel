# Staging Environment Variables  
# Staging Supabase for preview deployments

# Supabase Configuration (Create separate staging project)
VITE_SUPABASE_URL=https://staging-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=staging-anon-key

# Database
DATABASE_URL=staging-database-url

# Environment
NODE_ENV=production
VITE_ENV=staging

# Zoho Integration (Staging)
VITE_ZOHO_CLIENT_ID=staging-zoho-client-id
VITE_ZOHO_CLIENT_SECRET=staging-zoho-client-secret