# RESUME — exact next steps

Phase reached: **benchmarks frozen (v2, audited + repaired). Trials not yet run.**

## What exists
- `originals/` immutable (chmod a-w), `originals_manifest.json` = sha256 per file
- `contracts/<skill>.json` — 4 frozen, leak-checked, judge-safe
- `benchmarks/<skill>/suite.json` v2 (audited, repaired), `iteration.json` (6 tasks),
  `heldout.sealed.json` (4 tasks, chmod 400), `coverage.json` (dashboard-safe)
- `trials/payloads/<skill>.original.txt` — original skill text for contestant injection
- `dashboard/index.html` regenerated by `python3 build_dashboard.py`

## Selected skills
idea-validator · claude-md-guardian · promotional-campaigns · brand-design

## Conditions (see PROTOCOL.md)
- A = claude-sonnet-5 + original  **(DECLARED SUBSTITUTE for the unreachable Opus 4.8 — never label it Opus 4.8)**
- B = claude-opus-5 + no skill
- C = claude-opus-5 + original
- D = claude-opus-5 + candidate

## Next steps, in order
1. Baseline trials: run A, B, C on 3 iteration tasks per skill. Contestants are fresh
   agents, get ONLY their task + their condition's skill text, write output via Bash to
   `runs/<skill>/<trial-id>/output.md`.
2. Blind judging: fresh agent per comparison. Give ONLY task + evaluation_packet +
   anonymised outputs in randomised order. Never the skill file, model, or condition.
   Counterbalance: judge each pair twice with positions swapped, by different runs.
3. Feed blind judge feedback (not held-out anything) to a builder → candidate v1.
4. Run D, compare vs B and C. Iterate until green or retire.
5. Final clean evaluation on the sealed held-out set with fresh agents.

## Hard invariants
- Builders never see `heldout.sealed.json`, its packets, or held-out verdicts.
- Judges never see skill files, model identity, condition labels, or prior verdicts.
- `originals/` is never modified.
- Record the exact model id for every run.

## Known infrastructure issue (INC-001/003)
Subagent `Write` tool hangs intermittently. Two working paths:
  (a) agent writes via `Bash` + `python3 - <<'PY' ... json.dump(...) PY`
  (b) agent returns JSON as reply text, lead writes it
Always instruct one of these explicitly. If an agent stalls, check its transcript
mtime under /root/.claude/projects/.../subagents/ — flat size for >5min means hung;
TaskStop and relaunch.
