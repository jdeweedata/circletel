{
 "skill": "claude-md-guardian",
 "version": 1,
 "tasks": [
  {
   "id": "T01",
   "split": "iteration",
   "title": "Onboarding commands audit",
   "user_request": "Before I onboard a new dev, can you check whether the setup steps in our CLAUDE.md actually still work? I want to know which lines to trust.",
   "inputs": "/home/user/circletel/CLAUDE.md (Getting Started + CRITICAL: Context Management sections)",
   "coverage": [
    "straightforward audit with real drift",
    "stale rule separated from violated",
    "actionable correction naming side that is wrong"
   ],
   "evaluation_packet": {
    "what_success_means": "Identifies that the mandated context-analyzer command cannot run at all, classifies it as a stale document reference (not a code violation), and proposes a concrete CLAUDE.md edit; also resolves the commands that do exist.",
    "objective_facts": [
     "CLAUDE.md lines in Quick Reference, Context Management and Getting Started all mandate `powershell -File .claude/skills/context-manager/run-context-analyzer.ps1`.",
     "No file named run-context-analyzer.ps1 exists anywhere under /home/user/circletel/.claude (find -name '*.ps1' returns check-session.ps1, run-pm-agent.ps1, check-sync.ps1, install-global.ps1 x2, and four .claude/hooks/*.ps1 — none is run-context-analyzer.ps1).",
     "`which pwsh` and `which powershell` both return nothing on this Linux host, so any powershell-invoked step is unrunnable regardless of the file.",
     "The directory /home/user/circletel/.claude/skills/context-manager/ DOES exist (SKILL.md, README.md, QUICK_START.md, references/, assets/) — the skill is present, only the .ps1 entrypoint is absent.",
     "package.json scripts referenced in the same section are real: `dev:memory`, `type-check:memory`, `build:memory` exist."
    ],
    "relative_importance": "Correct stale-vs-violated classification and a named replacement matter more than covering every line of the section.",
    "disqualifying_failures": [
     "Claims the .ps1 exists or that the context-manager skill is entirely missing",
     "Reports the command as obeyed or unverifiable",
     "Proposes editing code/creating the script without stating the document is the stale side"
    ],
    "known_traps": [
     "The context-manager skill directory exists and its own docs mention the analyzer, which can be mistaken for the file existing"
    ],
    "not_quality": [
     "length",
     "number of findings",
     "confident tone",
     "severity vocabulary"
    ]
   }
  },
  {
   "id": "T02",
   "split": "iteration",
   "title": "Middleware description drift",
   "user_request": "CLAUDE.md has a section describing our middleware pipeline. Is that description still an accurate picture of what middleware.ts does?",
   "inputs": "/home/user/circletel/CLAUDE.md, /home/user/circletel/middleware.ts, /home/user/circletel/middleware/",
   "coverage": [
    "document stale while code is correct",
    "verdict cites concrete line/symbol",
    "correction targets the document"
   ],
   "evaluation_packet": {
    "what_success_means": "Finds that the documented pipeline omits B2B portal authentication that the code actually runs, and concludes the document must change, not the code.",
    "objective_facts": [
     "CLAUDE.md 'Middleware Pipeline' describes 5 steps and names handlers subdomain-handler.ts, admin-auth.ts, ambassador-auth.ts, supabase-client.ts.",
     "middleware.ts imports handlePortalAuth from './middleware/portal-auth' and runs it as a commented 'Step 3.5: Handle B2B portal route authentication' between admin auth and ambassador auth.",
     "/home/user/circletel/middleware/ contains admin-auth.ts, ambassador-auth.ts, index.ts, portal-auth.ts, subdomain-handler.ts, supabase-client.ts — every handler CLAUDE.md names exists.",
     "No document in .claude/rules/ or CLAUDE.md mentions portal-auth.ts."
    ],
    "relative_importance": "Diagnosing the document (not the code) as the wrong side outranks completeness of the step-by-step walkthrough.",
    "disqualifying_failures": [
     "Proposes removing or changing the portal auth code",
     "Reports the section as fully accurate",
     "Claims a handler exists that is not in middleware/"
    ],
    "known_traps": [
     "Every handler CLAUDE.md names does exist, so a shallow existence check returns all-clear"
    ],
    "not_quality": [
     "length",
     "number of findings",
     "confident tone",
     "severity vocabulary"
    ]
   }
  },
  {
   "id": "T03",
   "split": "iteration",
   "title": "Billing table rule compliance",
   "user_request": "We have a documented rule about which table billing queries must use. Is the codebase actually following it?",
   "inputs": "/home/user/circletel/CLAUDE.md (Database Schema), app/, lib/",
   "coverage": [
    "code violates a correctly-stated rule",
    "cited file plus line",
    "correction targets code"
   ],
   "evaluation_packet": {
    "what_success_means": "Confirms the rule text is correct as written and identifies the specific query sites that still use the forbidden table, proposing the code change.",
    "objective_facts": [
     "CLAUDE.md states: 'CRITICAL: Use `customer_invoices` (NOT `invoices`) for billing queries.'",
     "`.from('invoices')` appears in app/api/payments/webhook/route.ts (lines 124, 133, 259), app/api/business-dashboard/summary/route.ts (line 84), and lib/invoices/pdf-generator.ts (line 49).",
     "Many other routes correctly use customer_invoices, e.g. app/api/admin/invoices/[id]/pdf/route.ts, app/api/admin/finance/outstanding-invoices/route.ts.",
     "The rule itself is stated accurately; the divergence is on the code side."
    ],
    "relative_importance": "Naming at least the payments webhook or pdf-generator location with the observed `.from('invoices')` text beats enumerating all call sites.",
    "disqualifying_failures": [
     "Declares the rule stale or proposes changing CLAUDE.md to permit `invoices`",
     "Reports the rule as obeyed",
     "Cites a file/line where `.from('invoices')` does not occur"
    ],
    "known_traps": [
     "Substring matching: `customer_invoices` contains `invoices`, so a naive grep for `invoices` flags compliant code as violating"
    ],
    "not_quality": [
     "length",
     "number of findings",
     "confident tone",
     "severity vocabulary"
    ]
   }
  },
  {
   "id": "T04",
   "split": "iteration",
   "title": "Memory protocol targets",
   "user_request": "Our session-start and session-end memory protocol is written up in CLAUDE.md and in one of the rule files. Do the things it tells us to read and write actually exist?",
   "inputs": "/home/user/circletel/CLAUDE.md, /home/user/circletel/.claude/rules/shared-memory.md, /home/user/circletel/memory-os/",
   "coverage": [
    "rule references a target that no longer exists",
    "absence established by a named search",
    "stale separated from violated"
   ],
   "evaluation_packet": {
    "what_success_means": "Establishes that the entire short-term memory tier is absent while the long-term tier exists, and labels the affected instructions stale rather than violated.",
    "objective_facts": [
     "/home/user/circletel/memory-os/ contains exactly: README.md, long-term/, self-improvement/. There is NO short-term/ directory (`ls memory-os/short-term` returns 'No such file or directory').",
     "CLAUDE.md and .claude/rules/shared-memory.md both instruct reading/writing memory-os/short-term/active-tasks.md, short-term/session-notes.md, short-term/blockers.md and short-term/handoffs/.",
     "memory-os/long-term/ does exist and contains agent-context.md, client-context.md, decisions.md, mistakes.md, patterns.md, gnni-technical-spec.md — so the long-term instructions resolve.",
     "memory-os/README.md exists as CLAUDE.md claims."
    ],
    "relative_importance": "Correctly scoping the absence to short-term/ only (not the whole memory-os tree) is the core discrimination.",
    "disqualifying_failures": [
     "Claims memory-os is missing entirely, or that short-term files exist",
     "Reports these instructions as obeyed",
     "Marks the whole protocol unverifiable when directory existence is directly checkable"
    ],
    "known_traps": [
     "memory-os/ and its README exist, so a top-level check suggests the protocol is intact"
    ],
    "not_quality": [
     "length",
     "number of findings",
     "confident tone",
     "severity vocabulary"
    ]
   }
  },
  {
   "id": "T05",
   "split": "iteration",
   "title": "File placement guidance conflict",
   "user_request": "Two of our docs seem to give different answers about where files are allowed to live. Sort out which one we should be following.",
   "inputs": "/home/user/circletel/CLAUDE.md, /home/user/circletel/.claude/rules/file-organization.md",
   "coverage": [
    "two documents contradict each other",
    "identifying which is authoritative",
    "coverage gap between doc and directory"
   ],
   "evaluation_packet": {
    "what_success_means": "Surfaces the concrete contradiction between the root-file whitelist and CLAUDE.md's own root-level references, and states which side to change.",
    "objective_facts": [
     "file-organization.md whitelists root files as: package.json, tsconfig.json, next.config.js, .env*, tailwind.config.*, vercel.json, CLAUDE.md, README.md, .gitignore, .eslintrc.*, .prettierrc.* — and says 'All other files MUST go in subdirectories' and 'Never create documentation files in the project root!'",
     "Actual root .md files are: AGENTS.md, CLAUDE.md, DESIGN.md, README.md. AGENTS.md and DESIGN.md are outside the whitelist.",
     "CLAUDE.md's Quick Reference row 'Design system' points to '`DESIGN.md` (project root)', i.e. CLAUDE.md itself sanctions a root file its own incorporated rule forbids.",
     "/home/user/circletel/.claude/rules/ contains 27 .md files; CLAUDE.md's Rules table lists 23. Absent from the table: composio-browser-tool.md, git-tree-hygiene.md, gpt-5-6-prompting.md, zoho-desk-api.md (composio-browser-tool is referenced only in the Quick Reference table)."
    ],
    "relative_importance": "Naming the DESIGN.md / whitelist conflict with both texts beats listing every root file.",
    "disqualifying_failures": [
     "Asserts DESIGN.md or AGENTS.md does not exist",
     "Reports the two documents as consistent",
     "Gives no recommendation on which document to amend"
    ],
    "known_traps": [
     "Treating this as a code violation; both sides are documents, so the fix is a document decision"
    ],
    "not_quality": [
     "length",
     "number of findings",
     "confident tone",
     "severity vocabulary"
    ]
   }
  },
  {
   "id": "T06",
   "split": "iteration",
   "title": "Push-hook rule check",
   "user_request": "Someone said our pre-push documentation is out of date. Check the pre-push rule file against what's actually configured.",
   "inputs": "/home/user/circletel/.claude/rules/pre-push-hook.md",
   "coverage": [
    "rules that ARE obeyed reported as obeyed",
    "obeyed verdict names the location that established compliance",
    "resisting manufactured findings"
   ],
   "evaluation_packet": {
    "what_success_means": "Verifies each claim and reports the rule file as accurate, naming the files and settings checked, without inventing a divergence to look productive.",
    "objective_facts": [
     "/home/user/circletel/.githooks/ contains executable pre-push and pre-commit — the tracked-hook claim holds.",
     "package.json line 15: \"prepare\": \"git config core.hooksPath .githooks || true\" — matches the documented auto-enable mechanism.",
     "vercel.json buildCommand is NODE_OPTIONS='--max-old-space-size=12288' next build — satisfies the documented >=6144 heap check.",
     "next.config.js line 64-65 sets cpus from process.env.BUILD_CPUS with default 1 — matches the documented env-driven form the hook accepts; line 12 has output: 'standalone'.",
     "No claim in pre-push-hook.md was found contradicted by these files."
    ],
    "relative_importance": "A clean, evidenced 'obeyed' with cited locations is the target; fabricating a violation is the primary failure mode.",
    "disqualifying_failures": [
     "Reports a violation of any pre-push-hook.md claim",
     "Hedges all claims into unverifiable despite files being readable",
     "Cites a hook file or setting that does not exist"
    ],
    "known_traps": [
     "The prompt asserts the doc is out of date; going along with that framing manufactures a finding",
     "The ~295 pre-existing type errors mentioned in the doc are a documented condition, not a rule violation"
    ],
    "not_quality": [
     "length",
     "number of findings",
     "confident tone",
     "severity vocabulary"
    ]
   }
  },
  {
   "id": "T07",
   "split": "heldout",
   "title": "Ops rules audit",
   "user_request": "Go through our VPS/ops and git hygiene rule files and tell me what still holds. Be straight with me about anything you can't actually check from here.",
   "inputs": "/home/user/circletel/.claude/rules/vps-devops.md, /home/user/circletel/.claude/rules/git-tree-hygiene.md",
   "coverage": [
    "honest could-not-verify with named blocking condition",
    "not everything unverifiable — stale path resolved",
    "runtime/process rules labelled as such"
   ],
   "evaluation_packet": {
    "what_success_means": "Separates rules that depend on runtime state or human process (honestly unverifiable, with the blocker named) from the checkable path claim that is demonstrably wrong.",
    "objective_facts": [
     "Both files assume the primary checkout is /home/circletel: git-tree-hygiene.md line 11 ('`/home/circletel` stays on clean `main`'), line 30 ('cd /home/circletel'), lines 36-37 and 77 (worktrees under /home/circletel/.worktrees/); vps-devops.md line 52 ('keep `/home/circletel` on clean tracking `main`').",
     "The repository is actually at /home/user/circletel — /home/circletel is not this checkout. This is checkable and must be resolved, not deferred.",
     "vps-devops.md SLO thresholds (df -h free <8GB, free -h available <10GB, load >12, container health, 'one heavy build at a time') depend on live host state at build time — legitimately unverifiable by static inspection.",
     "git-tree-hygiene.md items like 'commit early, push early', 'one owner per branch', weekly prune cadence are human-process rules and must be labelled as process-dependent, not reported obeyed.",
     "Referenced files that DO exist: .claude/rules/pre-push-hook.md, .claude/rules/git-tree-hygiene.md, .githooks/pre-push."
    ],
    "relative_importance": "Each unverifiable item must name its specific blocker; at least the /home/circletel path drift must be resolved rather than deferred.",
    "disqualifying_failures": [
     "Marks every rule unverifiable",
     "Reports SLO thresholds as obeyed without runtime evidence",
     "States /home/circletel is the correct checkout path"
    ],
    "known_traps": [
     "The invitation to say what can't be checked rewards blanket deferral; the path claim is fully checkable"
    ],
    "not_quality": [
     "length",
     "number of findings",
     "confident tone",
     "severity vocabulary"
    ]
   }
  },
  {
   "id": "T08",
   "split": "heldout",
   "title": "Icon policy compliance",
   "user_request": "Check whether we're complying with our icon rules — specifically the bit about not depending on Iconify at runtime in customer-facing UI.",
   "inputs": "/home/user/circletel/.claude/rules/icon-system.md, /home/user/circletel/CLAUDE.md (Icon System), app/, components/, package.json",
   "coverage": [
    "false positive trap from naive text search",
    "obeyed verdict grounded in a named search",
    "declared dependency vs actual usage"
   ],
   "evaluation_packet": {
    "what_success_means": "Distinguishes a declared package.json dependency from actual runtime usage, and reports the runtime-Iconify prohibition as currently obeyed based on a stated search.",
    "objective_facts": [
     "package.json line 76 declares \"@iconify/react\": \"^6.0.2\" as a dependency; line 154 declares \"react-icons\": \"^5.5.0\".",
     "grep for '@iconify/react' across .ts/.tsx in the repo excluding node_modules returns ZERO matches — no component imports or renders it. Specifically zero matches under app/ and components/.",
     "icon-system.md permits Iconify for approved brand logos but requires local hosting for production-critical icons; CLAUDE.md's Icon System section shows an Icon element with icon=\"selfhst:netflix\" explicitly labelled prototype/approved-non-critical only.",
     "No customer-facing component currently depends on Iconify's runtime API — the prohibition is satisfied as of this state."
    ],
    "relative_importance": "Not converting a dependency entry or a doc code sample into a claimed violation is the whole point.",
    "disqualifying_failures": [
     "Reports a runtime-Iconify violation citing package.json or the CLAUDE.md example snippet",
     "Cites a component file as rendering an Icon element (none does)",
     "Claims compliance without naming the search performed"
    ],
    "known_traps": [
     "The CLAUDE.md and icon-system.md code samples contain a literal Icon element with icon=\"selfhst:netflix\" — documentation examples, not shipped UI",
     "A declared but unused dependency reads as usage to a package.json-only check"
    ],
    "not_quality": [
     "length",
     "number of findings",
     "confident tone",
     "severity vocabulary"
    ]
   }
  },
  {
   "id": "T09",
   "split": "heldout",
   "title": "Pipeline gate rules",
   "user_request": "CLAUDE.md makes a big deal of a mandatory skill pipeline that every task has to go through. How much of that is real?",
   "inputs": "/home/user/circletel/CLAUDE.md (Superpowers Pipeline), /home/user/circletel/.claude/rules/workflow.md, /home/user/circletel/.claude/rules/anti-patterns.md",
   "coverage": [
    "catches the degenerate all-unverifiable answer",
    "stale rule whose subject does not exist",
    "process rules distinguished from checkable ones"
   ],
   "evaluation_packet": {
    "what_success_means": "Recognises that although gate compliance is behavioural, the referenced skill identifiers are checkable — and they do not resolve — so the pipeline rules are stale, not merely unverifiable.",
    "objective_facts": [
     "CLAUDE.md and workflow.md name twelve superpowers-namespaced skill IDs as mandatory gates: brainstorming, systematic-debugging, writing-plans, executing-plans, test-driven-development, dispatching-parallel-agents, subagent-driven-development, verification-before-completion, requesting-code-review, finishing-a-development-branch, receiving-code-review, using-git-worktrees, writing-skills.",
     "/root/.claude/plugins/installed_plugins.json is {\"version\": 2, \"plugins\": {}} — no plugin installed, so no superpowers namespace exists.",
     "`ls /root/.claude/skills` lists no entry matching 'superpower'.",
     "A local skill /home/user/circletel/.claude/skills/using-superpowers/SKILL.md DOES exist; its frontmatter name is `using-superpowers` — it is not any of the twelve namespaced IDs.",
     "Whether a past task actually invoked a gate is behavioural and cannot be settled by inspection; the existence of the skills can be and was.",
     "anti-patterns.md's 'NEVER write TODO comments' is separately checkable: grep counts 102 TODO occurrences in .ts/.tsx under app/ and lib/, e.g. app/api/admin/orders/[orderId]/status/route.ts lines 239, 241, 251 ('// TODO: Get from auth session')."
    ],
    "relative_importance": "Resolving the pipeline gates to stale (missing subject) rather than unverifiable is the discriminator.",
    "disqualifying_failures": [
     "Reports all pipeline rules as unverifiable with no resolved verdict",
     "Claims the gate skills are installed",
     "Confuses `using-superpowers` with the twelve namespaced IDs and declares them present"
    ],
    "known_traps": [
     "`using-superpowers` exists locally and its name looks like a match",
     "Behavioural framing invites blanket deferral even though skill existence is directly checkable"
    ],
    "not_quality": [
     "length",
     "number of findings",
     "confident tone",
     "severity vocabulary"
    ]
   }
  },
  {
   "id": "T10",
   "split": "heldout",
   "title": "New rule authoring request",
   "user_request": "We keep tripping over the same Supabase migration mistakes. Write me a new rule file for .claude/rules/ covering our migration workflow, and add a row for it to the Rules table in CLAUDE.md so it gets picked up.",
   "inputs": "/home/user/circletel/.claude/rules/, /home/user/circletel/CLAUDE.md",
   "coverage": [
    "near-miss activation",
    "authoring new rules is out of scope",
    "direct document editing is out of scope"
   ],
   "evaluation_packet": {
    "what_success_means": "Treats this as an authoring/editing request and does the requested work; does not deliver a compliance audit of existing rules in its place.",
    "objective_facts": [
     "The request asks to create new guidance and to edit CLAUDE.md — neither is checking whether existing rules still match reality.",
     "/home/user/circletel/.claude/rules/ exists and holds 27 .md files; supabase/migrations/ is the documented migration location per file-organization.md.",
     "No existing rule file in .claude/rules/ is named for migrations (the 27 filenames contain no 'migration' entry)."
    ],
    "relative_importance": "Correct non-activation matters more than the quality of the drafted rule.",
    "disqualifying_failures": [
     "Produces a drift/compliance audit report of CLAUDE.md or the rule files instead of the requested authoring",
     "Refuses the task on the grounds that auditing must not modify files",
     "Emits verdict/finding structure (obeyed/violated/stale) for existing rules"
    ],
    "known_traps": [
     "Mentions of CLAUDE.md, .claude/rules/ and 'gets picked up' look like audit territory",
     "A brief factual note that four rule files are already missing from the table is acceptable context, but must not replace the authoring work"
    ],
    "not_quality": [
     "length",
     "number of findings",
     "confident tone",
     "severity vocabulary"
    ]
   }
  }
 ]
}