// Re-export shim — canonical implementation lives in components/backend/StatCard.tsx.
// Kept so existing `@/components/admin/shared/StatCard` imports keep working.
export { StatCard } from '@/components/backend/StatCard';
export type { StatCardProps } from '@/components/backend/StatCard';
