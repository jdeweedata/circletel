// Re-export shim — canonical implementation lives in components/backend/SectionCard.tsx.
// Kept so existing `@/components/admin/shared/SectionCard` imports keep working.
export { SectionCard } from '@/components/backend/SectionCard';
